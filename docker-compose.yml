services:
  backend:
    build:
      context: ./BackEnd
    container_name: deliveryplatform-backend
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - ./BackEnd/.env
    environment:
      NODE_ENV: production
      PORT: 4000


  mysql:
    image: mysql:8.4
    container_name: deliveryplatform-mysql
    restart: unless-stopped
    env_file:
      - ./BackEnd/.env
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./BackEnd/src/database/migrations:/docker-entrypoint-initdb.d:ro

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: deliveryplatform-phpmyadmin
    restart: unless-stopped
    ports:
      - "8080:80"
    env_file:
      - ./BackEnd/.env
    depends_on:
      - mysql

volumes:
  mysql-data:

networks:
  default:
    driver: bridge