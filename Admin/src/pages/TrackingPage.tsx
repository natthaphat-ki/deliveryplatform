export function TrackingPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold text-gray-900">Tracking</h2>
      <p className="mt-2 text-sm text-gray-500">Live map + WebSocket order tracking (Phase 10).</p>
    </div>
  );
}
